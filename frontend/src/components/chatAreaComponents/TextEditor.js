import Box from "@mui/material/Box";

export default function TextEditor() {
  return (
    <Box
      sx={{
        border: "1px solid #ccc",
        borderRadius: "5px",
        padding: "10px",
        height: "100px",
      }}
    >
      Hello from text Editor
    </Box>
  );
}
